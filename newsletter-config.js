// One-line edit after `wrangler deploy` to point /newsletter.html at the
// live worker. Until set, the form shows "endpoint not configured yet".
window.NEWSLETTER_API = ''; // e.g. 'https://depmedic-newsletter.depmedicdev.workers.dev'
